{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "wasm": "wasm-pack build --target web --out-dir ../web/src/wasm/pkg/ ../game-core/"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.0",
    "wasm-pack": "^0.13.1"
  }
}
